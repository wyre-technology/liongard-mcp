name: liongard_inventory_identities returns is_error when credentials missing
server:
  command: node
  args: [dist/index.js]
assert:
  tool: liongard_inventory_identities
  args: {}
  expect:
    is_error: true
timeout: 30s
